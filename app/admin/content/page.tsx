'use client';import {useState} from 'react';export default function Content(){const [title,setTitle]=useState('');const [text,setText]=useState('');const [result,setResult]=useState('');const [loading,setLoading]=useState(false);async function generate(){setLoading(true);const r=await fetch('/api/ai',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({type:'seo',title,text})});const d=await r.json();setResult(d.result||d.message||'');setLoading(false)}return <main><section className="page-hero"><div className="container"><span className="eyebrow">دستیار محتوا</span><h1>تولید محتوای فارسی با هوش مصنوعی</h1><p>ساخت متن پروژه و SEO با اتصال امن به سرویس DeepSeek.</p></div></section><section className="section"><div className="container"><div className="card form"><input placeholder="عنوان پروژه یا خدمت" value={title} onChange={e=>setTitle(e.target.value)}/><textarea placeholder="متن یا اطلاعات خام پروژه" value={text} onChange={e=>setText(e.target.value)}/><button className="btn" onClick={generate} disabled={loading}>{loading?'در حال تولید...':'تولید محتوای SEO'}</button>{result&&<div className="feature"><strong>خروجی هوش مصنوعی</strong><br/><span style={{whiteSpace:'pre-wrap'}}>{result}</span></div>}</div></div></section></main>}
