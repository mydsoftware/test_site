'use client';import {useEffect,useState} from 'react';
type R={id:string;name:string;phone:string;email:string;projectType:string;budget:string;start:string;message:string;createdAt:string};
export default function Requests(){const [items,setItems]=useState<R[]>([]);useEffect(()=>{fetch('/api/requests').then(r=>r.json()).then(setItems)},[]);return <main><section className="page-hero"><div className="container"><span className="eyebrow">سرنخ‌های فروش</span><h1>درخواست‌های مشاوره</h1><p>درخواست‌های ثبت‌شده از فرم سایت.</p></div></section><section className="section"><div className="container"><div className="admin-table">{items.map(r=><article className="card" key={r.id} style={{marginBottom:16}}><h3>{r.name}</h3><p>{r.phone} · {r.email}</p><p>{r.projectType} · {r.budget} · {r.start}</p><p>{r.message}</p></article>)}{!items.length&&<div className="empty-state">هنوز درخواستی ثبت نشده است.</div>}</div></div></section></main>}
