'use client';
import Link from 'next/link';import {useEffect,useMemo,useState} from 'react';
type Project={id:string;title:string;category:string;location:string;year:string;status:string;description:string;image:string};
export default function ProjectBrowser(){const [projects,setProjects]=useState<Project[]>([]);const [filter,setFilter]=useState('همه');const [query,setQuery]=useState('');useEffect(()=>{fetch('/api/projects').then(r=>r.json()).then(setProjects)},[]);const categories=['همه',...Array.from(new Set(projects.map(p=>p.category)))];const filtered=useMemo(()=>projects.filter(p=>(filter==='همه'||p.category===filter)&&`${p.title} ${p.location} ${p.status}`.includes(query)),[projects,filter,query]);return <><div className="project-filters"><input aria-label="جستجوی پروژه" placeholder="جستجوی پروژه..." value={query} onChange={e=>setQuery(e.target.value)}/>{categories.map(c=><button key={c} className={filter===c?'active':''} onClick={()=>setFilter(c)}>{c}</button>)}</div><div className="grid-3">{filtered.map(p=><Link className="project" key={p.id} href={`/projects/${p.id}`} style={{backgroundImage:`url(${p.image})`}}><div className="project-info"><h3>{p.title}</h3><p>{p.category} · {p.location} · {p.status}</p></div></Link>)}</div>{!filtered.length&&<p className="empty-state">پروژه‌ای با این مشخصات پیدا نشد.</p>}</>}
