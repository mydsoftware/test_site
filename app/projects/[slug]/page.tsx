import {notFound} from 'next/navigation';import Link from 'next/link';import {projects} from '@/data/projects';
export function generateStaticParams(){return projects.map(p=>({slug:p.id}));}
export default async function Project({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=projects.find(x=>x.id===slug);if(!p)notFound();return <><section className="page-hero"><div className="container"><span className="eyebrow">پروژه {p.category}</span><h1>{p.title}</h1></div></section><section className="section"><div className="container project-detail"><img src={p.image} alt={p.title}/><div className="project-meta"><div><small>موقعیت</small><strong>{p.location}</strong></div><div><small>سال اجرا</small><strong>{p.year}</strong></div><div><small>وضعیت</small><strong>{p.status}</strong></div></div><h2>درباره پروژه</h2><p>{p.description}</p><p>این پروژه با رویکردی یکپارچه در طراحی، مدیریت و اجرا انجام شده است. جزئیات فنی و انتخاب متریال با توجه به کاربری، شرایط سایت و اهداف کارفرما انجام شده‌اند.</p><div className="actions"><Link className="btn" href="/contact">درخواست مشاوره مشابه</Link><Link className="btn secondary" href="/projects">بازگشت به پروژه‌ها</Link></div></div></section></>}
