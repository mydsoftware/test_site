import {notFound} from 'next/navigation';import Link from 'next/link';import {readProjects} from '@/lib/project-store';
export const dynamic='force-dynamic';
export default async function Project({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const projects=await readProjects();const p=projects.find(x=>x.id===slug);if(!p)notFound();return <><section className="page-hero"><div className="container"><span className="eyebrow">نمونه‌کار تایپ‌نو · {p.category}</span><h1>{p.title}</h1><p>نمونه‌ای از خدمات حرفه‌ای تایپ، تبدیل محتوا و آماده‌سازی اسناد تایپ‌نو.</p></div></section><section className="section"><div className="container project-detail"><img src={p.image} alt={p.title}/><div className="project-meta"><div><small>نوع خدمت</small><strong>{p.category}</strong></div><div><small>سال</small><strong>{p.year}</strong></div><div><small>وضعیت</small><strong>{p.status}</strong></div></div><h2>درباره این نمونه‌کار</h2><p>{p.description}</p><p>در این پروژه، فایل‌های اولیه با استفاده از ابزارهای مدرن پردازش و سپس از نظر تایپ، قالب‌بندی، یکدستی و کیفیت خروجی بازبینی شده‌اند تا فایل نهایی آماده استفاده باشد.</p><div className="actions"><Link className="btn" href="/contact">ثبت سفارش مشابه</Link><Link className="btn secondary" href="/projects">بازگشت به نمونه‌کارها</Link></div></div></section></>}
